services:
  workbench:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      # 可选：设置后打开页面需要输入访问密码
      - ACCESS_PASSWORD=${ACCESS_PASSWORD:-}
    restart: unless-stopped
